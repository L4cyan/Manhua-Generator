# Native rendering (no ComfyUI). Installed automatically by start.bat when an
# NVIDIA GPU is present. torch itself is installed separately with the right
# CUDA wheel index -- see start.bat.
diffusers>=0.31
transformers>=4.44
accelerate>=0.34
safetensors>=0.4
peft>=0.13
sentencepiece>=0.2
